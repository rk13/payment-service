buildscript {
    repositories {
        jcenter()
    }
    dependencies {
        classpath 'com.github.jengelman.gradle.plugins:shadow:2.0.4'
    }
}

subprojects {
    apply plugin: 'maven'
    apply plugin: 'java'

    group = 'payments'
}

